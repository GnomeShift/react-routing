import React from 'react'
import {Link} from "react-router-dom";

function Contacts() {
    return <div>
        <h2>Контакты</h2>
        <div>
            <Link to="/">Главная</Link>
        </div>
    </div>
}

export default Contacts;
